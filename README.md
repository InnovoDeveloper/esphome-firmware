# Innovo ESPHome Firmware

Public firmware distribution for Innovo IoT devices.

This repo contains OTA firmware binaries (`firmware/`) and display assets (`va/icons/`) served to ESP32 devices over HTTPS. The source code and device configs live in a private repository.

Do not modify files here directly — updates are synced from the private source repo using `espsyncfw`.
